import { NumTarjetaPipe } from './num-tarjeta.pipe';

describe('NumTarjetaPipe', () => {
  it('create an instance', () => {
    const pipe = new NumTarjetaPipe();
    expect(pipe).toBeTruthy();
  });
});
